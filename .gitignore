# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# impeccable design skill — keep config.json (hook settings) and surfaces/
# (direction contracts), but not generated review screenshots/cache
.impeccable/review/
.impeccable/hook.cache.json

# Playwright MCP tool output (transient QA screenshots/snapshots)
.playwright-mcp/

# Local AI coding-assistant skill/tool installs (per-machine, not site code —
# reinstall anytime with `npx skills add` / `npx impeccable install`)
.agent/
.agents/
.claude/
.codex/
